_schema-version: 3.3.0
ID: CAP-sample
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: CAP-sample-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: CAP-sample-auth
      - name: CAP-sample-db
      - name: CAP-sample-destination

  - name: CAP-sample-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: CAP-sample-db

  - name: CAP-sample-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: CAP-sample-auth
      - name: srv-api
      - name: CAP-sample-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: CAPsamplecreateorders
          artifacts:
            - bookshopcreateorders.zip
          target-path: app/
        - name: CAPsamplemanagebooksandorders
          artifacts:
            - bookshopmanagebooksandorders.zip
          target-path: app/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: CAPsamplecreateorders
    type: html5
    path: app/createorders
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: CAPsamplemanagebooksandorders
    type: html5
    path: app/managebooksandorders
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: CAP-sample-destinations
    type: com.sap.application.content
    requires:
      - name: CAP-sample-auth
        parameters:
          service-key:
            name: CAP-sample-auth-key
      - name: CAP-sample-html5-repo-host
        parameters:
          service-key:
            name: CAP-sample-html5-repo-host-key
      - name: srv-api
      - name: CAP-sample-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: CAP-sample-html5-repository
              ServiceInstanceName: CAP-sample-html5-repo-host
              ServiceKeyName: CAP-sample-html5-repo-host-key
              sap.cloud.service: CAPsample.service
            - Name: CAP-sample-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: CAP-sample-auth
              ServiceKeyName: CAP-sample-auth-key
              sap.cloud.service: CAPsample.service

resources:
  - name: CAP-sample-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: CAP-sample-${org}-${space}
        tenant-mode: dedicated
  - name: CAP-sample-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: CAP-sample-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: CAP-sample-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: CAP-sample-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
